# Mood receipt — Claude preferences

## Auto-push
After every file edit or write, automatically stage and push changes to `main` branch.
Hook configured in `.claude/settings.local.json` (PostToolUse on Edit|Write).
